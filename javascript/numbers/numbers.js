let altura = 170;
let altura_Float = 1.70;
let peso = 80.5;

const altura_red = Math.ceil(altura_Float)
const peso_red = Math.floor(peso)

const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE
console.log(sonIguales)